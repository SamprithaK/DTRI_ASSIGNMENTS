#include <stdio.h>
#include <stdlib.h>
#include<math.h>
char a='a';char b='B';
int main()
{
    printf("Enter 2 letters\n");
   // scanf("%c%c",&a,&b);
    (abs(a-b)==32)?(printf("both are equal")):(printf("both are not equal"));
    return 0;
}
