#include <stdio.h>
#include <stdlib.h>
int a=5;
int main()
{if (a==0)
    {
    printf("The number is 0\n");}
    (a>0)?(printf("the number is positive")):(printf("the number is negative"));
    return 0;
}
