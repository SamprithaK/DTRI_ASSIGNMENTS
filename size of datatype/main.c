#include <stdio.h>
#include <stdlib.h>
int main()
{
    printf("the size of charcacter  is %d\n",sizeof(char));
    printf("the size of int datatype is %d\n",sizeof(int));
      printf("the size of float datatype is %d\n",sizeof(float));
        printf("the size of long int datatype is %d\n",sizeof(long int));
          printf("the size of unsigned long int datatype is %d\n",sizeof(unsigned long int));
    return 0;
}
